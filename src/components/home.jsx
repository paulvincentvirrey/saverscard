import React from "react";
import header from "../img/banner.png";
import services from "../img/services.png";

const Home = () => {
  return (
    <React.Fragment>
      <header id="header">
        <img src={header} />
      </header>
      {/* <section id="services">
        <br />
        <img src={services} />
      </section>
      <section id="about">
        <br />
      </section>
      <section id="contact">
        <br />
      </section> */}
    </React.Fragment>
  );
};

export default Home;
